// file_download_stub.dart — Native Desktop / Mobile fallback

void downloadFileBytes(List<int> bytes, String fileName) {
  // On native desktop/mobile, received files are saved directly to local storage directory
}
